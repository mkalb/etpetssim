/**
 * Core simulation contracts and assembly utilities shared by concrete simulations.
 *
 * <p>This package provides simulation instance abstractions and factory entry points.
 * It is organized into four sub-packages: {@code model} (domain state and rules),
 * {@code view} (JavaFX scene-graph and rendering), {@code viewmodel} (JavaFX properties,
 * bindings, UI state), and {@code shared} (layer-neutral types used by two or more layers).
 */
@org.jspecify.annotations.NullMarked
package de.mkalb.etpetssim.simulations.core;
